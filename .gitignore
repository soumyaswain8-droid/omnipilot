# Build artifacts
build/
.build/
DerivedData/

# Xcode
*.xcuserdata
xcuserdata/
*.xcworkspace
!*.xcodeproj

# Models (large binary files — download via setup.sh)
models/*.bin
models/*.onnx
models/minilm_*

# OS
.DS_Store

# Python
__pycache__/
*.pyc

# Temp
*.wav
*.tmp

# Native whisper.cpp build + local cmake (rebuild via docs/whisper-speed.md)
vendor/

# Meeting captures — private transcripts, notes, OCR of on-screen content.
# NEVER commit: contains third-party speech, sales calls, and OCR'd screenshots
# of whatever was on screen (terminals, browser tabs, credentials).
data/

# Runtime logs (may contain transcript fragments)
logs/

# Parked/archived model weights
models/*.park
